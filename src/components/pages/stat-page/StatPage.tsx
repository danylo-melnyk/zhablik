import { FC } from 'react';
import { Box } from '@mui/material';

import * as styles from './StatPage.styles';

const StatPage: FC = () => {
  return <Box sx={styles.wrapper}> </Box>;
};

export default StatPage;
